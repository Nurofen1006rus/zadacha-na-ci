#include <stdio.h>
#include <math.h>

int main(){
   double a, b, c,Vsr, V, t, s;
   printf("Первая скорость точки u0:  ");scanf("%lf", &a);
   printf("Вторая сторость точки u1:  ");scanf("%lf", &b);
   printf("Третья скорость точки u2:  ");scanf("%lf", &c);
   printf("В первой половине пути скорость равна u0, \n среднюю скорость во второй половине пути обозначу через u', \n она выражается как арифметическое среднее скоростей u1 и u2:\n");
   Vsr = (a+b)/2;
   printf("Среднее арифметическое: ");
   printf("%.2lf\n", Vsr);
   printf("Весь путь обозначу как s. Половина пути: s/2.\nИскомую среднюю скорость на всем пути обозначу как Uср,\n время движения на всем пути как t. Тогда:");
   V = 2*a*(b+c)/(2*a+b+c);
   printf("\nСредняя за все время движения скорость точки ");
   printf("%.2lf\n", V);
}
